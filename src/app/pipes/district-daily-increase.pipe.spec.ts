import { DistrictDailyIncreasePipe } from './district-daily-increase.pipe';

describe('DistrictDailyIncreasePipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictDailyIncreasePipe();
    expect(pipe).toBeTruthy();
  });
});
