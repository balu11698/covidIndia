import { StateDailyIncreasePipe } from './state-daily-increase.pipe';

describe('StateDailyIncreasePipe', () => {
  it('create an instance', () => {
    const pipe = new StateDailyIncreasePipe();
    expect(pipe).toBeTruthy();
  });
});
